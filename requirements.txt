# ── Core data science ────────────────────────────────────────────────────────
pandas>=1.5
numpy>=1.23
scipy>=1.9
scikit-learn>=1.1
statsmodels>=0.13

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib>=3.6
seaborn>=0.12

# ── File I/O ──────────────────────────────────────────────────────────────────
openpyxl>=3.0
xlrd>=2.0

# ── Report generation ─────────────────────────────────────────────────────────
jinja2>=3.0
pylatex>=1.4

# ── Enterprise dependencies (SQLAlchemy, Alembic, Database, Redis, Celery) ────
sqlalchemy>=2.0
alembic>=1.12
psycopg2-binary>=2.9
redis>=5.0
celery>=5.3

# ── API dependencies (FastAPI, Uvicorn, Pydantic, Auth) ──────────────────────
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.7
pydantic-settings>=2.3
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
httpx>=0.27
python-multipart>=0.0.9
python-dotenv>=1.0
structlog>=24.1
typer>=0.12

# ── Email validation for Pydantic EmailStr ───────────────────────────────────
email-validator>=2.1

# ── Development & testing ─────────────────────────────────────────────────────
pytest>=7.0
pytest-cov>=4.0
pytest-asyncio>=0.21
ruff>=0.5
mypy>=1.5
faker>=20.0